# プロジェクト概要
- 言語・API: C++、DirectX 12
- ジャンル: 3Dレールシューティング（スプラトゥーンのレール上のミニステージを参考）

# 開発環境
- Visual Studio 2026

# 今後の開発予定（優先順位順）
1. 弾の発射・当たり判定システム（最優先）
   - プレイヤーが弾（プロジェクタイル）を発射できるようにする
   - 発射した弾と的オブジェクトの当たり判定を実装する（現状はレティクルと的の重なりのみで判定している簡易実装のため、本実装に置き換える）
2. 撃破演出の追加
   - 的の撃破時に簡易的な演出（エフェクト等）を入れる
3. RailEditor：レールの複数化対応
   - 2本目以降のレールを新規作成・切り替えできるようにする
4. （優先度低・後回し）JSONホットリロード機能
   - 実行中にImGuiで変更した値を、そのままファイルへ保存・反映できるようにする

# 現状の実装状況・足りない点（随時更新）
- 実装済み：RailEditorでの制御点の追加、Position のドラッグ／数値入力による移動、Save/LoadによるJSON保存・読込
- 実装済み：RailEditorでの制御点の削除機能（削除後のインデックス再計算含む）
- 実装済み：プレイヤー（メインカメラ）が制御点間を実際に移動するロジック本体
- 実装済み：各Pointに設定したSpeed値を実際のプレイヤー移動に反映する処理
- 実装済み：的オブジェクトの配置（静的表示、プレースホルダーモデル）
- 実装済み：レティクルと的の当たり判定・命中時の的消滅処理（簡易実装：レティクルと的の重なりのみで判定。実際の弾発射による判定ではないため要置き換え）
- 未確認・要実装：制御点のRotation値をImGui上で数値編集する機能
- 未確認・要実装：ImGui上で制御点をHierarchy的な一覧として選択・表示する機能
- 未実装：プレイヤーが弾を発射する処理、および弾自体と的の当たり判定
- 未着手：撃破演出（エフェクト等）
- 未着手：2本目以降のレール作成・切り替え

# 直近のタスク（日程目安）
- 8/12まで：RailEditorでレールを新しく作れるようにする（2本目のレール作成）
- 8/15まで：プレイヤーが弾を発射し、その弾で的との当たり判定を取る処理（現状のレティクル重なり判定からの置き換え）

# コーディング規約
- 命名規則・インデント: 一般的なC++の規約に準拠すること。
- コメントアウト: 処理の意図や内容をなるべく細かく具体的に記述すること。

# 実装時の特記事項・禁止事項
- マジックナンバーの使用を禁止する。必ず定数（constexpr等）を使用すること。
- 無駄な処理を何度も繰り返すような、非効率なコーディングをしないこと。

# AIへの指示（厳守事項）
- 回答は短く簡潔にすること。
- 聞かれたこと以外の余計なコードを追加しないこと。
- コードの順番を勝手に変更しないこと。
- 変更箇所にはわかりやすい目印をつけること（ただし特殊な記号は使用しない）。
- コメントアウトに不自然な記号を使用しないこと。
- 提示されたコードを省略して返すのは禁止。
- 「整理して」と指示された場合は、処理内容は一切変更せず、不要なコメントの削除、わかりやすいコメントの追加、インデントの改善のみ行うこと（見やすさのための順番変更は許可する）。