# プロジェクト概要
- 言語・API: C++、DirectX 12
- ジャンル: 3Dレールシューティング（スプラトゥーンのレール上のミニステージを参考）

# 開発環境
- Visual Studio 2026

# 今後の開発予定（優先順位順）
1. レール間を移動できるようにする（最優先）
   - 複数存在するレール間でプレイヤー（カメラ）が乗り移れるようにする
2. （優先度低・後回し）JSONホットリロード機能
   - 実行中にImGuiで変更した値を、そのままファイルへ保存・反映できるようにする

# 現状の実装状況・足りない点（随時更新）
- 実装済み：RailEditorでの制御点の追加、Position のドラッグ／数値入力による移動、Save/LoadによるJSON保存・読込
- 実装済み：RailEditorでの制御点の削除機能（削除後のインデックス再計算含む）
- 実装済み：プレイヤー（メインカメラ）が制御点間を実際に移動するロジック本体
- 実装済み：各Pointに設定したSpeed値を実際のプレイヤー移動に反映する処理
- 実装済み：的オブジェクトの配置（静的表示、プレースホルダーモデル）
- 実装済み：プレイヤーが弾（プロジェクタイル）を発射する処理、および発射した弾と的オブジェクトの当たり判定（レティクル重なり判定から置き換え済み）
- 実装済み：RailEditorでの2本目以降のレールの新規作成・切り替え
- 実装済み：制御点のRotation値をImGui上で数値編集する機能（RailEditor.cpp）
- 実装済み：的（Target）をHierarchy的な一覧として選択・表示する機能（GameScene.cpp）
- 未実装：RailEditorの制御点をHierarchy的な一覧として選択・表示する機能（現状はText表示のみ、選択状態なし）
- 実装済み：撃破演出（GameScene.cpp）。ParticleManagerのUpdate/Drawをゲームループに接続し、的の撃破時にEmitSparkで火花パーティクルを発生させる
- 未着手：レール間の移動（SwitchActiveRailはエディタUI専用で、Playモード中のロジックからは未使用）

# 直近のタスク（日程目安）
- 8/23まで：レール間を移動できるようにする

# コーディング規約
- 命名規則・インデント: 一般的なC++の規約に準拠すること。
- コメントアウト: 処理の意図や内容をなるべく細かく具体的に記述すること。

# 実装時の特記事項・禁止事項
- マジックナンバーの使用を禁止する。必ず定数（constexpr等）を使用すること。
- 無駄な処理を何度も繰り返すような、非効率なコーディングをしないこと。

# AIへの指示（厳守事項）
- 回答は短く簡潔にすること。
- 聞かれたこと以外の余計なコードを追加しないこと。
- コードの順番を勝手に変更しないこと。
- 変更箇所にはわかりやすい目印をつけること（ただし特殊な記号は使用しない）。
- コメントアウトに不自然な記号を使用しないこと。
- 提示されたコードを省略して返すのは禁止。
- 「整理して」と指示された場合は、処理内容は一切変更せず、不要なコメントの削除、わかりやすいコメントの追加、インデントの改善のみ行うこと（見やすさのための順番変更は許可する）。